# This archive is digest-verified: campaign manifests record SHA-256 values for
# driver scripts, result records and vendored modules. Any line-ending
# normalization would change those bytes and break every recorded digest, so
# git must store and check out every file exactly as committed.
* -text
