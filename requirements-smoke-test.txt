# Minimal dependencies for the quick check:
#
#     python verify_snapshot.py
#     python audit_manifest_digests.py
#     python fetch_data.py
#
# This is NOT the environment any campaign ran in. The spherical-harmonic
# kernel needs only these two packages; the digest audit and the data fetcher
# need nothing beyond the standard library. To reproduce a campaign, install
# the matching file under environments/ instead.
numpy==2.2.6
numba==0.63.1
