# Python 3.12.1 (MSC v.1937 64 bit, Windows 11 x64)
#
# Campaigns: R8 (alpha margin), R14 (budget Pareto, trajectories, oracle,
# timing, variational), R15 (cadence, deployable calibration, fixed oracle),
# R16 (multibody calibration), R17 (sixty-day long arcs), and the R18-R23
# campaigns that reuse R14 trajectories.
#
# Versions are those recorded in the provenance blocks of the result records
# these campaigns wrote.
numpy==2.2.6
scipy==1.14.1
numba==0.63.1
spiceypy==8.0.0
matplotlib==3.9.0

# Used only by the independent spectrum cross-checks.
pyshtools==4.14.1
